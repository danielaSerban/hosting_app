<h2>Clients</h2>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @clients.each do |client| %>
      <tr>
        <td><%= client.name %>&nbsp&nbsp</td>
        <td><%= link_to 'Show', client %>&nbsp&nbsp</td>
        <td><%= link_to 'Edit', edit_client_path(client) %>&nbsp&nbsp</td>
        <td><%= link_to 'Destroy', client, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Client', new_client_path %>
